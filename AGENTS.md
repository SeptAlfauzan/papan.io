## Workflow rules

TODO
[] If you change business logic in lib/**, update or add the matching test in test/**.
[] If you change a feature's behavior, update docs/features/<feature>.md. if <feature>.md is not exist, create new one.
[] Any changes for methods, variables, class, add proper documentation of it
